# URL base de la API. Se lee en tiempo de build (Vite).
# En produccion la define Render; en local se deja vacia y el proxy de Vite
# redirige /api al backend indicado en VITE_DEV_API.
VITE_API_URL=

# Backend al que apunta el proxy durante `npm run dev`.
VITE_DEV_API=http://127.0.0.1:8000
